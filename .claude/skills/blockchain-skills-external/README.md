# Blockchain Skills (External)

69+ blockchain-focused skills from
[hairyf/blockchain-skills](https://github.com/hairyf/blockchain-skills).

This directory is a placeholder. Populate it by cloning the upstream repo into
this folder:

```bash
# from the workspace root
rm -rf .claude/skills/blockchain-skills-external
git clone https://github.com/hairyf/blockchain-skills.git \
  .claude/skills/blockchain-skills-external
```

Update later with:

```bash
cd .claude/skills/blockchain-skills-external && git pull origin main
```

## Key skills (once cloned)
hardhat · solidity · openzeppelin · foundry · alchemy · dex · uniswap · aave · …

> Not cloned automatically during setup (requires network access and would nest
> a second git repo). Run the clone above when you want the full library.
